services:
  docker-http:
    ports:
      - 80:80
    build: .
    restart: unless-stopped 
    volumes:
      - ~/certs/atom-airforce.crt:/usr/local/apache2/conf/server.crt:ro
      - ~/certs/atom-airforce.key:/usr/local/apache2/conf/server.key:ro
    secrets:
      - ssl-csr
      - ssl-key
